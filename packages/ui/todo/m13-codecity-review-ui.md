# M13 — CodeCity & Review UI

> Источник: `packages/ui/TODO.md`

> **Задач (ui):** 22 | **Проверка:** CodeCity 2D treemap, diff viewer, graph visualization

> **Результат milestone:** Готовы Review UI и CodeCity visual flows для навигации по качеству кода.

## v0.8.0 — Интерфейс review

> Diff viewer, comment thread, SSE streaming, rule editor. ~80K tokens.

> **Результат версии:** Завершена версия «v0.8.0 — Review UI» в рамках M13; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                              | Статус | Результат                                                                                                                                                                                                                                                                                                                 | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------ | ----------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-COMP-005 | Реализовать code diff viewer        | DONE   | Реализация: Side-by-side. Inline comments. Syntax highlighting. Virtualized. Реализован `CodeDiffViewer` с side-by-side сравнением, инлайн комментариями per-line и virtualized-скроллом по строкам через `useVirtualizedList`; добавлен парсер-обновление синтаксиса для базовой подсветки и fallback для пустого диффа. | Реализация: Side-by-side. Inline comments. Syntax highlighting. Virtualized. Готово, если: для WEB-COMP-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                  |
| WEB-COMP-006 | Реализовать review comment thread   | DONE   | Реализация: Nested comments. Reply. Resolve. Feedback buttons. Реализован `ReviewCommentThread` с вложенной структурой комментариев, локальным reply/add flow, переключаемыми like/dislike, кнопкой resolve/unresolve и визуальными статусами resolved.                                                                   | Реализация: Nested comments. Reply. Resolve. Feedback buttons. Готово, если: для WEB-COMP-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                |
| WEB-COMP-007 | Реализовать sSE streaming component | DONE   | Реализован SSE streaming component с EventSource, auto-reconnect, прогресс-индикатором и обработкой ошибок (`SseStreamViewer`). Реализовано через `useSSEStream` hook + `SseStreamViewer` с тестами на прогресс, событие и reconnect.                                                                                     | Реализация: EventSource. Auto-reconnect. Progress display. Error handling. Готово, если: для WEB-COMP-007 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                    |
| WEB-COMP-008 | Реализовать rule editor (TipTap)    | DONE   | Реализован `RuleEditor` с toolbar (bold/italic/heading/code block), переключателем предпросмотра, счетчиком символов и markdown preview через динамический импорт; добавлено интеграционное подключение на `SettingsCodeReviewPage`; добавлены component и integration тесты.                                             | Реализация: Moved to v0.12.0. TipTap (только OSS core, без Pro extensions). Rich text. Code blocks. Markdown. Dynamic import. Готово, если: для WEB-COMP-008 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |

---

## v0.9.0 — Real-time & Data Hooks

> SSE transport, React Query hooks for permissions, reviews, rules. ~60K tokens.

> **Результат версии:** Завершена версия «v0.9.0 — Real-time & Data Hooks» в рамках M13; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                       | Статус | Результат                                                                                                                                                                                                                                                                                                                                                                                                                                     | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------ | ---------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-HOOK-003 | Реализовать useSSE()         | DONE   | Реализован транспорт на `useSSEStream` для EventSource со стабилизацией reconnect, маппингом событий и прогрессом.                                                                                                                                                                                                                                                                                                                            | Реализация: SSE transport layer. Auto-reconnect. Event type multiplexing. Реализовано в v0.8.0 через `useSSEStream` и интеграцию `SseStreamViewer`. Готово, если: для WEB-HOOK-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                   |
| WEB-HOOK-004 | Реализовать usePermissions() | DONE   | Реализован `PermissionsApi`, `usePermissionsQuery`, `IPermissionsResponse`, RBAC guards и тесты (`permissions-endpoint`, `use-permissions-query`): роли нормализуются и кеш invalidation срабатывает при смене role key; добавлен базовый MSW-мок для `/api/v1/permissions`.                                                                                                                                                                  | Реализация: React Query hook для RBAC permissions. Cache invalidation при role change. Готово, если: для WEB-HOOK-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                |
| WEB-HOOK-005 | Реализовать useCodeReview()  | DONE   | Реализован `CodeReviewApi` + `useCodeReview` hook: загружает review по ID, запускает review и отправляет feedback, с инвалидацией query-кэша; добавлены endpoint и hook интеграционные тесты, обновлены локальные msw-фикстуры для `/api/v1/reviews/*`.                                                                                                                                                                                       | Реализация: React Query hook. Fetch review, trigger review, submit feedback. Готово, если: для WEB-HOOK-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                          |
| WEB-HOOK-006 | Реализовать useCustomRules() | DONE   | Реализован `useCustomRules()` с endpoint + optimistic CRUD (`create`, `update`, `delete`), нормализацией scope/status и query-ключей; добавлены endpoint и hook-тесты для успешной валидации UI-критериев.                                                                                                                                                                                                                                    | Реализация: React Query hook. CRUD для custom rules. Optimistic updates. Готово, если: для WEB-HOOK-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                              |
| WEB-HOOK-007 | Реализовать useAnalytics()   | DONE   | Реализован `AnalyticsSdk` + `AnalyticsProvider`/`useAnalytics`: типизированные события (funnel step, drop-off, key action, time-to-first-value), auto correlation/session IDs, batching + sampling, offline queue, redaction и consent-gating с очисткой очереди по отказу. Реализован в `packages/ui/src/lib/analytics/analytics-sdk.ts`, `packages/ui/src/lib/analytics/analytics-context.tsx`, интеграция в `packages/ui/src/app/app.tsx`. | Реализация: UX telemetry tracking hook/SDK (OSS-only, без обязательного SaaS): типизированная схема событий (funnel time-to-first-value, drop-offs, key actions), единый Context provider, автоподстановка tenant/user/session correlation ids, batching + sampling, offline queue, redaction (no PII/code by default), opt-out/consent gating. Готово, если: события отправляются детерминированно (без дубликатов при retry), не содержат чувствительных данных по умолчанию, корректно работают при offline/online переходах, и поддерживают сценарии продуктовой аналитики (time-to-first-value, drop-offs) без деградации производительности UI; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |

---

## v0.10.0 — Graph Visualization Part 1

> XYFlow (`@xyflow/react`) + dagre, file-level view, function graph. ~100K tokens.

> **Результат версии:** Завершена версия «v0.10.0 — Graph Visualization Part 1» в рамках M13; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID            | Задача                                 | Статус | Результат                                                                                                                                                                                                                                                                                                                 | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------- | -------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-GRAPH-001 | Реализовать XYFlow graph component     | DONE   | Реализован универсальный `XyFlowGraph`-компонент с lazy-рендерингом @xyflow/react, auto-layout через Dagre и базовой стилизацией под интерфейс; добавлен слой расчёта layout (`calculateGraphLayout`) + unit-тесты на корректность позиции и устойчивость к невалидным рёбрам.                                            | Реализация: Graph visualization на базе XYFlow (`@xyflow/react`) + auto-layout (`@dagrejs/dagre`). Dynamic import. Готово, если: для WEB-GRAPH-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-GRAPH-002 | Реализовать file-level dependency view | DONE   | Реализован `FileDependencyGraph` в `RepositoryOverviewPage`: добавлены зависимости файлов/связей для 3 репозиториев, рендеринг графа в секции dashboard и пустое состояние при отсутствии данных; интеграционные и компонентные тесты добавлены в `repository-overview.page.test.tsx` и `file-dependency-graph.test.tsx`. | Реализация: File dependencies graph. Готово, если: для WEB-GRAPH-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                               |
| WEB-GRAPH-003 | Реализовать function/class call graph  | DONE   | Релизован Function/Class Call Graph (nodes/calls + интеграция в обзор репозитория + фильтрация + компонентные + интеграционные тесты).                                                                                                                                                                                    | Реализация: Call relationships. Готово, если: для WEB-GRAPH-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                    |
| WEB-GRAPH-004 | Реализовать модуль/package view        | DONE   | Реализован `PackageDependencyGraph` с графом зависимостей модулей, связями runtime/peer, интерактивным поиском, интегрирован в обзор репозитория и покрыт component + integration тестами.                                                                                                                                | Реализация: Package dependencies. Готово, если: для WEB-GRAPH-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                  |
| WEB-GRAPH-005 | Реализовать interactive controls       | DONE   | Реализованы viewport- и relation filters controls для package graph: интерактивный zoom/pan, кнопки fit-view, кнопки фильтрации runtime/peer и компонентное + интеграционное покрытие.                                                                                                                                    | Реализация: Zoom, pan, filter controls. Готово, если: для WEB-GRAPH-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                            |

---

## v0.21.0 — CodeCity 2D — Treemap Visualization

> 2D treemap кодовой базы с метриками, CCR impact overlay, issue heatmap. ~120K tokens.

> **Результат версии:** Завершена версия «v0.21.0 — CodeCity 2D — Treemap Visualization» в рамках M13; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                                | Статус | Результат                                                                                                                                                                                                                                                                                                        | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------ | ------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-CITY-001 | Реализовать treemap component         | DONE   | Реализован `CodeCityTreemap`, нормализующий file-level данные в иерархию package->files, рисующий responsive treemap по LOC-подобному размеру (приоритет `loc`, затем `size`, затем `complexity`), и отображающий summary (packages/files/LOC). Компонент интегрирован в `RepositoryOverviewPage`.               | Реализация: D3 treemap layout. Nested rectangles = packages/files. Size = LOC. Responsive. Готово, если: для WEB-CITY-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.  |
| WEB-CITY-002 | Реализовать metric color encoding     | DONE   | Реализован `CodeCityTreemap` с поддержкой переключаемой метрики color-by (`complexity`, `coverage`, `churn`), нормализованным диапазоном цвета (зелёный→красный) и легендой. Цветовая индикация пересчитывается для выбранной метрики; компонент покрыт unit-тестами.                                            | Реализация: Color = complexity/coverage/churn. Legend. Configurable metric selector. Готово, если: для WEB-CITY-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.        |
| WEB-CITY-003 | Реализовать CCR Impact overlay        | DONE   | Реализованы уровни CCR влияния (`changed`, `impacted`, `ripple`) в `CodeCityTreemap`: приоритизация источников (флаг уровня или `impactedFiles`), подсветка stroke/legend и агрегированная сводка `impactSummary`. Добавлены unit-тесты на метаданные `impactType`.                                              | Реализация: Highlight affected files in treemap при просмотре CCR. Changed/impacted/ripple. Готово, если: для WEB-CITY-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-CITY-004 | Реализовать issue heatmap layer       | DONE   | Реализован heatmap-оверлей для `CodeCityTreemap`: поддержка `issueCount` в файлах, нормализация интенсивности относительно `maxIssuesPerFile`, оверлей через полупрозрачный цветной fill на ячейках, legend (gradient green → red + summary), а также unit-тесты на агрегацию, `issueSummary` и наличие легенды. | Реализация: Overlay: density of found issues по файлам. Gradient: green → red. Готово, если: для WEB-CITY-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.              |
| WEB-CITY-005 | Реализовать interactive drill-down    | DONE   | Реализован интерактивный drill-down CodeCity treemap: клик по пакету переключает вид на файлы пакета, добавлены breadcrumb и кнопка Back, обновлены component+unit тесты под новый путь.                                                                                                                         | Реализация: Click package → zoom into files. Breadcrumb navigation. Back button. Готово, если: для WEB-CITY-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.            |
| WEB-CITY-006 | Реализовать tooltip with file metrics | DONE   | Реализован hover-панель с LOC, complexity, coverage, last review, issue count + quick link через резолвер `fileLink`. Реализован тест на hover-сценарий в CodeCityTreemap и обновлены проверки treemap data.                                                                                                     | Реализация: Hover: LOC, complexity, coverage, last review, issue count. Quick link to file. Готово, если: для WEB-CITY-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-CITY-007 | Реализовать temporal comparison       | DONE   | Реализован temporal comparison в `CodeCityTreemap` с поддержкой `compareFiles`, вычислением `LOC` delta, маркерами роста/снижения в ячейках, сравнит. сводкой и линией `LOC delta` в tooltip. `buildCodeCityTreemapData` получил вычисление added/removed/changed, а UI-тесты покрывают новый сценарий.          | Реализация: Compare treemap between two commits/dates. Highlight growth/shrinkage. Готово, если: для WEB-CITY-007 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.          |
| WEB-CITY-008 | Реализовать codeCity dashboard page   | DONE   | Добавлена страница `CodeCityDashboardPage` с репозиторным фильтром и выбором метрики, маршрут `/dashboard/code-city` с `AuthBoundary`, пункт меню в Sidebar и тест страницы (`code-city-dashboard.page.test.tsx`).                                                                                               | Реализация: Dedicated page: /dashboard/code-city. Metric selector. Репозиторий filter. Готово, если: для WEB-CITY-008 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.      |

---
