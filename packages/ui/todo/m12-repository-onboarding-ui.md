# M12 — Repository Onboarding UI

> Источник: `packages/ui/TODO.md`

> **Задач (ui):** 9 | **Проверка:** Repository onboarding wizard

> **Результат milestone:** Готов onboarding UX для подключения репозиториев и запуска сканирования.

## v0.23.0 — Репозиторий Onboarding UI

> UI для онбординга репозиториев и визуализации прогресса сканирования. ~60K tokens.

> **Результат версии:** Завершена версия «v0.23.0 — Repository Onboarding UI» в рамках M12; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID            | Задача                                        | Статус | Результат | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------- | --------------------------------------------- | ------ | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| WEB-ONBRD-001 | Реализовать onboardingWizard                  | DONE   | Готово   | Реализация: Multi-step wizard: paste URL → configure → scan. Progress stepper. Validation at each step. Готово, если: для WEB-ONBRD-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                           |
| WEB-ONBRD-002 | Реализовать scanProgressPage                  | DONE   | Готово   | Реализация: Real-time scan progress via SSE. Phase indicators. ETA estimation. Logs panel. Готово, если: для WEB-ONBRD-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                        |
| WEB-ONBRD-003 | Реализовать репозиторийListPage               | DONE   | Готово   | Реализация: List onboarded repositories. Status badges (scanning/ready/error). Search, filter, sort. Готово, если: для WEB-ONBRD-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                              |
| WEB-ONBRD-004 | Реализовать репозиторийOverviewPage           | DONE   | Готово   | Реализация: Post-scan dashboard: tech stack, architecture summary, key metrics, health score. Готово, если: для WEB-ONBRD-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                      |
| WEB-ONBRD-005 | Реализовать rescanScheduleDialog              | DONE   | Готово   | Реализация: Configure periodic rescan schedule. Cron picker. Last scan timestamp. Готово, если: для WEB-ONBRD-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                 |
| WEB-ONBRD-006 | Реализовать onboardingEmptyState              | DONE   | Готово   | Реализация: Empty state for dashboard when no repos onboarded. CTA to start onboarding wizard. Готово, если: для WEB-ONBRD-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                    |
| WEB-ONBRD-007 | Реализовать scanErrorRecovery                 | DONE   | Готово   | Реализация: UI для ошибочных сканов на списке репозиториев — показ частичных результатов, раскрытие деталей ошибки и кнопка повторного скана с callback. Готово, если: для WEB-ONBRD-007 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                           |
| WEB-ONBRD-008 | Реализовать bulk onboarding mode (enterprise) | DONE   | Готово | Реализация: В wizard добавить режим bulk: импорт списка репозиториев из провайдера, multi-select (20+), применение общего шаблона (rules/schedule/notifications), запуск параллельных scan jobs и единая страница прогресса с ошибками по каждому repo. Готово, если: админ может подключить и запустить скан для десятков репозиториев за один проход без повторяющихся кликов, прогресс/ошибки прозрачно видны на одном экране, а UX выдерживает частичные фейлы (retry per-repo, pause/cancel); DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-ONBRD-009 | Реализовать onboarding templates registry     | DONE   | Готово | Реализация: Реестр шаблонов онбординга: именованные templates (rules preset, scan cadence, notification defaults, tags), версия/описание, preview “что будет применено”, возможность применить шаблон к выбранным репозиториям и откатить изменения (audit-friendly). Готово, если: пользователь может повторно применять одинаковую конфигурацию онбординга без ручного переноса настроек, есть безопасный preview/confirm шаг, и поведение детерминированно при повторном применении/откате; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.     |

---
