# M14 — All Providers & Pages

> Источник: `packages/ui/TODO.md`

> **Задач (ui):** 44 | **Проверка:** Additional pages, settings, analytics

> **Результат milestone:** Готовы дополнительные страницы и настройки для полного пользовательского сценария.

## v0.11.0 — Additional Pages Part 1

> Issues, Integrations, Webhooks, Token usage, Org settings. ~100K tokens.

> **Результат версии:** Завершена версия «v0.11.0 — Additional Pages Part 1» в рамках M14; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                            | Статус | Результат | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------ | --------------------------------- | ------ | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-PAGE-006 | Реализовать issues tracking       | DONE   | Добавлена страница `IssuesTrackingPage` (`/issues`) с фильтрами поиска/статуса/критичности, virtual-scrolling списком и inline actions; добавлен маршрут `/issues` и пункт меню `Issues`, добавлены тесты на фильтры и inline action callback. | Реализация: List issues. Virtual scrolling. Filter by severity, status. Inline actions. Готово, если: для WEB-PAGE-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-PAGE-007 | Реализовать integrations          | DONE   | Добавлены `SettingsIntegrationsPage` и route `/settings-integrations`: карточки Jira/Linear/Sentry/Slack со статусом подключения (connected/degraded/disconnected), health-check, connect/disconnect и configure (workspace/target/sync/notifications + save). Интеграции добавлены в settings navigation и quick links. | Реализация: Jira, Linear, Sentry, Slack. Connection status. Configure. Готово, если: для WEB-PAGE-007 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                  |
| WEB-PAGE-008 | Реализовать webhook management    | DONE   | Добавлены `SettingsWebhooksPage` и route `/settings-webhooks`: create/delete endpoint, secret rotation, enable/disable, test delivery, status chips, а также virtualized список endpoint-ов и virtualized delivery logs. Страница подключена в settings navigation и quick links. | Реализация: List webhooks. Create/delete. Secret rotation. Logs. Virtual scrolling. Готово, если: для WEB-PAGE-008 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.     |
| WEB-PAGE-009 | Реализовать token usage           | DONE   | Добавлены `SettingsTokenUsagePage` и route `/settings-token-usage`: date range filter (`24h/7d/30d/90d`), KPI summary, табы usage by model/developer/CCR и расчет estimated cost по model-pricing. Страница подключена в settings navigation и quick links. | Реализация: Usage by model, developer, CCR. Cost estimate. Date range. Готово, если: страница token usage корректно считает usage/cost по выбранному периоду и совпадает с данными backend API на контрольной выборке; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                  |
| WEB-PAGE-010 | Реализовать organization settings | DONE   | Добавлены `SettingsOrganizationPage` и route `/settings-organization`: профиль организации (name/slug/timezone), billing блок (plan/status/seats/payment method + retry/confirm action), members management (invite/change role/remove), BYOK секция и таблица audit logs. Страница подключена в settings navigation и quick links. | Реализация: Name, billing, members. BYOK. Audit logs. Готово, если: billing-настройки сохраняются идемпотентно, критичные действия подтверждаются, а ошибки оплаты отображаются пользователю без потери введённых данных; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                               |

---

## v0.12.0 — Additional Pages Part 2

> Team, Rules, Audit, SAML, BYOK. ~100K tokens.

> **Результат версии:** Завершена версия «v0.12.0 — Additional Pages Part 2» в рамках M14; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID               | Задача                                                                    | Статус | Результат | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------- | ------------------------------------------------------------------------- | ------ | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-PAGE-011     | Реализовать team management                                               | DONE   | Добавлены `SettingsTeamPage` и route `/settings-team`: создание команды, приглашение участников по email, assignment репозиториев и role assignment в команде; страница добавлена в settings navigation и quick links, добавлен page-test на ключевой флоу. | Реализация: Create team. Add members. Assign repos. Role assignment. Готово, если: для WEB-PAGE-011 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                |
| WEB-PAGE-012     | Реализовать rules library                                                 | DONE   | Добавлены `SettingsRulesLibraryPage` и route `/settings-rules-library`: browse pre-built rules с фильтрами, import в активный профиль, создание custom rules и встроенный test-run с результатом; пункт добавлен в settings navigation и quick links, добавлен page-test на ключевой пользовательский флоу. | Реализация: Browse pre-built rules. Import. Create custom. Test rules. Готово, если: для WEB-PAGE-012 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                              |
| WEB-PAGE-013     | Реализовать audit logs                                                    | DONE   | Добавлены `SettingsAuditLogsPage` и route `/settings-audit-logs`: виртуализированный список изменений, фильтры по actor/action/date range и экспорт отфильтрованного набора в CSV; экран подключен в settings navigation и quick links, добавлен page-test ключевого флоу. | Реализация: List changes. Virtual scrolling. Filter by actor, action. Date range. Export. Готово, если: для WEB-PAGE-013 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                           |
| WEB-PAGE-014     | Реализовать sAML provider management                                      | DONE   | Добавлены `SettingsSsoPage` и route `/settings-sso`: SAML и OIDC конфигурация с валидацией обязательных полей, masked secret preview, сохранение конфигураций и отдельные test SSO действия для каждого провайдера; экран подключен в settings navigation и quick links, добавлен page-test ключевого сценария. | Реализация: SAML config. OIDC config. Test SSO. Готово, если: SAML/OIDC настройка проходит master-data валидацию, тест SSO даёт диагностируемый результат success/failure и не сохраняет секреты в открытом виде; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                   |
| WEB-PAGE-015     | Реализовать bYOK management                                               | DONE   | Добавлены `SettingsByokPage` и route `/settings-byok`: добавление API keys по провайдеру, masked secret display, ротация/деактивация/удаление ключей и usage stats (общие + per-provider); экран подключен в settings navigation и quick links, добавлен page-test ключевого сценария. | Реализация: Add API keys. Mask display. Rotation. Usage stats. Готово, если: BYOK ключи маскируются в UI, rotation не ломает активные интеграции, а удаление/обновление ключа корректно отражается в usage-метриках; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                |
| WEB-THEME-001    | Реализовать appearance settings (HeroUI-like)                             | DONE   | Добавлены `SettingsAppearancePage` и route `/settings-appearance`: mode toggle (light/dark/system), preset selection с preview-карточками, reset to default и live preview без перезагрузки; экран подключен в settings navigation и quick links, добавлен page-test ключевого сценария. | Реализация: Страница Settings -> Appearance с переключением mode (light/dark/system), выбором preset theme с preview-карточками как у HeroUI, reset to default и live preview без перезагрузки. Готово, если: пользователь может изменить тему из настроек за <= 2 клика, выбор сохраняется и применяется глобально ко всем страницам, доступность (keyboard/focus/aria/contrast) подтверждена тестами; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                             |
| WEB-THEME-002    | Реализовать advanced theme controls                                       | DONE   | В `SettingsAppearancePage` добавлены advanced controls: accent color picker + intensity slider, base palette picker (neutral/warm/cool), global/form radius sliders и live preview; изменения применяются мгновенно через CSS variables (`--accent`, `--background`, `--surface`, `--radius-*`) без reload и сохраняются в localStorage, добавлен расширенный page-test на ключевой сценарий. | Реализация: В Appearance добавить расширенные контролы как в HeroUI demo: Accent color picker/slider, Base palette picker (нейтральная/теплая/холодная), радиусы компонентов (global radius + form radius) с live preview на стандартном наборе компонентов. Готово, если: изменения Accent/Base/Radius применяются мгновенно без reload, отражаются на всех HeroUI компонентах и проходят a11y-проверки контраста/фокуса; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                          |
| WEB-THEME-003    | Реализовать random theme picker + quick presets                           | DONE   | В `SettingsAppearancePage` добавлены quick preset chips (включая популярные варианты), random preset trigger с hotkey `Alt+R`, preview перед применением и детерминированный undo последнего random выбора; random выбирает только контрастно-допустимые preset варианты, добавлено расширенное page-test покрытие сценария. | Реализация: Добавить кнопку random preset (горячая клавиша), быстрые chips с популярными пресетами (Default, Sky, Lavender, Mint и т.д.), preview перед применением и undo последнего случайного выбора. Готово, если: random не генерирует недоступные по контрасту темы, undo работает детерминированно, а quick presets переключаются за один клик; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                              |
| WEB-THEME-004    | Реализовать user theme library                                            | DONE   | В `SettingsAppearancePage` реализована user theme library: create/rename/duplicate/delete пользовательских тем, экспорт/импорт JSON с schema validation и детерминированным разрешением конфликтов имён, pin/apply favorite preset, локальное восстановление через storage и синхронизация библиотеки с profile API (`/api/v1/user/settings|preferences`) с fallback на local persistence. | Реализация: Сохранение пользовательских тем (create, rename, duplicate, delete), экспорт/импорт JSON темы, pin favorite preset и синхронизация библиотеки между устройствами через profile API. Готово, если: пользователь может восстановить свои темы после logout/login, импорт валидируется по схеме без падений UI, а конфликты имён разрешаются предсказуемо; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                 |
| WEB-NOTIF-001    | Реализовать notifications inbox & delivery preferences                    | DONE   | Добавлены `SettingsNotificationsPage` и route `/settings-notifications`: единый inbox с типами событий (review completed/drift alert/prediction alert), mark read/unread и deep-link в контекст; реализованы delivery preferences по каналам Slack/Discord/Teams/In-app и in-app mute rules с quiet hours, пункт подключён в settings navigation и quick links, добавлен page-test ключевого потока. | Реализация: Единый Notification Center в UI: inbox лента, типы событий (review completed, drift alert, prediction alert), mark as read/unread, deep-link в целевой экран; настройки каналов Slack/Discord/Teams и in-app mute rules. Готово, если: пользователь получает уведомления в inbox без дубликатов, может перейти по deep-link в контекст события и управлять предпочтениями доставки по каналам; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                          |
| WEB-SAFE-001     | Реализовать safeguard decision trace panel                                | DONE   | В `CcrReviewDetailPage` добавлена панель `SafeGuard decision trace`: показаны применённые фильтры (`dedup/hallucination/severity`), список замечаний с итогом `shown/filtered out`, причины скрытия и детальный pipeline по каждому шагу фильтра; добавлен page-test на ключевой сценарий прозрачного trace. | Реализация: На экране review добавить панель объяснения SafeGuard: какие фильтры применены (dedup/hallucination/severity), какие замечания отфильтрованы и почему. Готово, если: для каждого замечания доступен прозрачный trace filter pipeline и пользователь понимает причину скрытия/показа без обращения к backend логам; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                      |
| WEB-FDBK-001     | Реализовать reviewer feedback learning loop UI                            | DONE   | В `CcrReviewDetailPage` добавлен блок `Reviewer feedback learning loop`: quick actions (`false positive`/`irrelevant`/`duplicate`), отправка feedback в 2 клика с `Accept`/`Reject`, явный статус применения (`accepted/rejected`) и причина отклонения, а также история feedback с привязкой duplicate-замечаний к canonical trace; page-test расширен под ключевой сценарий. | Реализация: UX для accepted/rejected feedback с причиной отклонения, quick actions (false positive, irrelevant, duplicate), и визуализацией того, что обратная связь учтена системой. Готово, если: пользователь может отправить feedback за <= 2 клика, статус применения feedback отображается явно, а повторные замечания корректно связываются с историей решения; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                              |
| WEB-TEN-001      | Реализовать organization switcher & tenancy guard UX                      | DONE   | В `Header` добавлен глобальный organization/workspace switcher с явным текущим контекстом, в `DashboardLayout` добавлены подтверждение при переключении tenant, tenant-scoped storage invalidation и route-level tenancy guard (fallback на безопасный маршрут при недопустимом deep-link); обновлены layout-тесты на switch + guard сценарий. | Реализация: Глобальный переключатель организации/workspace с явным текущим контекстом, подтверждение при переключении, и блокировка смешивания данных между tenant contexts в UI-кэше и URL state. Готово, если: смена организации мгновенно обновляет все данные и фильтры, old-tenant данные не остаются в интерфейсе, а deep-links корректно валидируются по правам tenant-а; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                    |
| WEB-RBAC-001     | Реализовать role-based access states and action gating                    | DONE   | Добавлен единый слой UI-RBAC policy (`viewer/developer/lead/admin`) с action-level решениями `enabled/disabled/hidden` и причинами (`ui-policy.ts`); в `DashboardLayout/Header` добавлен role preview switcher, а на критичных экранах (`CcrReviewDetailPage`, `SettingsTeamPage`) включены disable/hidden gating + policy hints; расширены page/layout тесты на restricted states и role switch сценарии. | Реализация: Единый слой UI-политик по ролям (viewer/developer/lead/admin), disable/hidden states для restricted actions, понятные причины отказа доступа, и policy hints в критичных формах. Готово, если: запрещенные действия консистентно скрыты/заблокированы на всех экранах и пользователь видит объяснение недоступности без 500/unknown ошибок; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                             |
| WEB-NAV-001      | Реализовать global navigation IA + route guard map                        | DONE   | Добавлен формализованный route tree + guard matrix (`auth/tenant/role`) в `route-guard-map.ts`, подключены breadcrumb strategy и route-level guard в `DashboardLayout`, в `Header` добавлен global route search shortcut (`Ctrl+K` + submit), обновлены layout/unit тесты на quick search и guard behavior. | Реализация: Формализованный route tree для UI (основные разделы, settings subroutes, analytics workspaces), breadcrumb strategy, global search shortcuts и guard matrix (auth/tenant/role). Готово, если: навигация предсказуема на desktop/mobile, все route transitions соответствуют guard-правилам, а broken/dead-end routes отсутствуют; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                       |
| WEB-BILL-001     | Реализовать billing lifecycle and entitlement states UI                   | DONE   | Добавлены `SettingsBillingPage` и route `/settings-billing`: lifecycle состояния `trial/active/past_due/canceled`, paywall banners для `past_due/canceled`, deterministic lock/unlock premium feature entitlements по plan+status, подтверждаемый downgrade и явный outcome действий, плюс история изменений плана/статуса; экран подключён в settings navigation и quick links, добавлен page-test ключевого потока. | Реализация: Состояния trial/active/past-due/canceled, paywall banners, feature lock/unlock UX, upgrade/downgrade confirmation и история изменений плана. Готово, если: ограничения premium-фич отображаются детерминированно по entitlement, апгрейд/даунгрейд сценарии имеют явный outcome, а данные биллинга не расходятся с backend состоянием; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                  |
| WEB-JOB-001      | Реализовать operations job monitor and recovery center                    | DONE   | Добавлены `SettingsJobsPage` и route `/settings-jobs`: монитор long-running jobs (`review/scan/analytics`) со статусом, ETA, retry counter, paused/stuck/failed состояниями, ручными recovery actions (`retry/cancel/requeue`), error drill-down по выбранной job и recovery audit trail; экран подключён в settings navigation и quick links, добавлен page-test ключевого сценария. | Реализация: UI для long-running jobs (review, scan, analytics): статус, ETA, retries, paused/stuck state, ручной retry/cancel/requeue с audit trail и error drill-down. Готово, если: пользователь видит жизненный цикл job в реальном времени, может безопасно восстановить зависший процесс, а действия оператора логируются и воспроизводимы; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                    |
| WEB-SESSION-001  | Реализовать session recovery UX (P0)                                      | DONE   | В `DashboardLayout` добавлен forced re-auth modal для 401/419 (`codenautic:session-expired`), autosave draft через capture input listener в session storage и восстановление pending intent после re-auth; добавлен session recovery helper (`session-recovery.ts`) и layout-test на сценарий истечения сессии с восстановлением draft + redirect в pending route. | Реализация: Forced re-auth modal при 401/419 в активной работе, autosave черновика (forms/comments), восстановление pending intent после повторной авторизации. Готово, если: при истечении сессии пользователь не теряет введённые данные, повторная отправка выполняется детерминированно, а status и ошибки сессии объясняются в UI; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                             |
| WEB-CONCUR-001   | Реализовать concurrent config edit resolver (P0)                          | DONE   | Добавлены `SettingsConcurrencyPage` и route `/settings-concurrency`: optimistic concurrency control по `etag`, conflict dialog с field-level diff, решения `merge/reload/retry`, explicit outcome и audit trail решений; экран подключён в settings navigation и quick links, добавлен page-test конфликта и его резолюции. | Реализация: Optimistic concurrency control для админ-настроек (version/etag), conflict dialog с diff полей, варианты merge/reload/retry и аудит принятого решения. Готово, если: silent overwrite невозможен, конфликт между двумя операторами корректно детектируется и завершается предсказуемым outcome без потери валидных данных; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                              |
| WEB-POLICY-001   | Реализовать runtime policy drift handling (P0)                            | TODO   | Не начато | Реализация: Реакция UI на mid-session change ролей/entitlements: invalidate permissions cache, action gating без падений, безопасный fallback route и уведомление пользователя. Готово, если: при изменении прав в процессе работы запрещённые действия немедленно блокируются/скрываются с объяснением, а открытые страницы не переходят в undefined/500 состояния; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                |
| WEB-LINK-001     | Реализовать deep-link security guard (P0)                                 | TODO   | Не начато | Реализация: Guard для ссылок из inbox/Slack/email: проверка tenant+role+resource access, sanitize query params, выбор switch-org или безопасного deny/fallback. Готово, если: deep-link не открывает чужой tenant-контекст и не исполняет небезопасные параметры, а пользователь получает понятный путь продолжения; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                |
| WEB-OUTAGE-001   | Реализовать provider degradation mode UX (P0)                             | TODO   | Не начато | Реализация: Глобальные degraded banners, статус affected features, режим queue/retry для критичных действий, быстрые ссылки на runbook и incident state. Готово, если: при частичном outage внешних провайдеров интерфейс остаётся работоспособным, пользователь видит ограниченные функции и ожидаемое время восстановления без ложных success-состояний; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                          |
| WEB-BULK-001     | Реализовать bulk actions with undo and audit (P1)                         | TODO   | Не начато | Реализация: Multi-select + batch actions для notifications/issues/reviews, optimistic update с undo-таймером, server reconciliation и audit trail. Готово, если: массовые действия применяются атомарно или с понятной частичной деградацией, undo работает детерминированно, а аудит фиксирует who/when/what; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                      |
| WEB-PRIV-001     | Реализовать privacy-safe export and redaction UX (P1)                     | TODO   | Не начато | Реализация: Client-side проверка на секреты/PII перед copy/export/share, подсветка чувствительных фрагментов, redaction suggestions и подтверждение безопасной версии. Готово, если: API keys/tokens/emails/secrets не уходят в экспорт без явного подтверждения, redaction работает предсказуемо, а пользователь получает объяснение причин блокировки; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                            |
| WEB-MTAB-001     | Реализовать multi-tab consistency guard for tenant/theme/permissions (P1) | TODO   | Не начато | Реализация: Синхронизация состояния между вкладками через BroadcastChannel/storage events: tenant switch, theme change, permission snapshot update; безопасная инвалидация tenant-кэша, re-check route guards и non-blocking уведомления о применённой синхронизации. Готово, если: во второй вкладке изменения контекста применяются без hard reload, stale tenant данные не отображаются, а права/доступы пересчитываются детерминированно без race-condition артефактов; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                         |
| WEB-E2E-001      | Реализовать системный e2e regression suite для a11y/i18n (P0)             | TODO   | Не начато | Реализация: Единый e2e набор для критичных маршрутов (dashboard/reviews/settings): keyboard-only navigation, focus management, ARIA roles/names/landmarks, screen reader flow assertions и проверки длинных локализаций/pseudo-locale snapshots. Интеграция в CI с merge-blocking при регрессии и артефактами диагностики. Готово, если: регрессии a11y/i18n детектируются автоматически до merge, длинные строки не ломают layout/действия, а отчеты тестов дают воспроизводимые шаги и скриншоты для исправления; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-CONTRACT-001 | Реализовать import/export contract validation (P2)                        | TODO   | Не начато | Реализация: Валидация schema/version для импортируемых JSON (themes/rules), migration hints, preview before apply и actionable error messages. Готово, если: несовместимые форматы не ломают UI, поддерживаемые версии корректно мигрируются/применяются, а пользователь видит точную причину отказа по контракту; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                  |
| WEB-COMP-008     | Реализовать rule editor (TipTap)                                          | TODO   | Не начато | Реализация: TipTap (только OSS core, без Pro extensions). Rich text. Code blocks. Markdown. Dynamic import (lazy load TipTap). Готово, если: для WEB-COMP-008 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                      |

---

## v0.12.1 — Enterprise UX Hardening

> Activation checklist, My Work/Triage, enterprise tables, freshness/provenance, timezone scheduling, explainability, help, state system, personalization.

> **Результат версии:** UX-план становится пригодным для больших компаний: быстрый onboarding до first value, предсказуемый triage, доверие к данным и единые паттерны объяснимости/состояний.

| ID            | Задача                                                        | Статус | Результат | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------- | ------------------------------------------------------------- | ------ | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-ACT-001   | Реализовать activation checklist и setup path (P0)            | TODO   | Не начато | Реализация: На Dashboard и в Settings добавить Activation Checklist (прогресс 0-100%) с детерминированным списком шагов (connect git/llm provider, invite teammates, SSO, добавить repo, запустить первый scan, настроить notifications, baseline rules/dry-run) + deep-links в нужные экраны. Шаги role-aware (admin vs developer) и показывают blockers/предусловия. Persist состояния (server + local fallback) и безопасная возможность dismiss. Готово, если: новый org может пройти к «first value» (первый успешный scan + видимые insights/issue) по чеклисту без внешней помощи, все шаги имеют ясный статус/причину блокировки, deep-links корректны, a11y/responsive подтверждены, и есть покрытие минимум component+integration для state machine чеклиста; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-INBOX-001 | Реализовать unified "My Work / Triage" hub (P0)               | TODO   | Не начато | Реализация: Route-level страница "My Work" объединяет: assigned CCRs, критичные issues, inbox notifications, stuck jobs, pending approvals. Приоритизация по severity/urgency, быстрые фильтры (mine/team/repo), keyboard shortcuts, deep-link в контекст, inline actions (mark read, assign, snooze, open review). Использует bulk actions (если доступно) и realtime обновления (poll/SSE where possible). Готово, если: пользователь может закрыть ежедневный triage без переключения между 4-5 страницами, все элементы имеют понятный источник/временную метку, действия не теряют контекст при навигации назад, и a11y/keyboard-навигация покрывает основной поток; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                               |
| WEB-INBOX-002 | Реализовать triage ownership model (assigned/SLA/escalation)  | TODO   | Не начато | Реализация: Формализовать модель ownership для элементов triage: owner (user/team), статус (unassigned/assigned/in-progress/blocked/done), SLA/due-date, escalation policy (warnings + notify), и единый UI-паттерн отображения (badge + tooltip + quick assign). Интеграция в `WEB-INBOX-001` + audit trail для изменений. Готово, если: triage элементы не превращаются в “свалку” (каждый item имеет явного владельца или reason why), просрочки/риски видны заранее, есть быстрые действия assign/snooze/escalate, и права (RBAC) соблюдаются для назначения/эскалаций; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                             |
| WEB-TBL-001   | Реализовать enterprise data table ergonomics kit (P0)         | TODO   | Не начато | Реализация: Единый DataTable kit на базе TanStack Table (`@tanstack/react-table`) + TanStack Virtual (без paid grid решений) для всех list-страниц (CCRs/issues/audit/token usage/repos): виртуализация, sticky header, column pin/reorder/hide/resize, density (compact/comfortable), row selection + bulk bar, keyboard navigation (roving tabindex), доступные row actions, column-level filtering, экспорт CSV/JSON, saved views (persist per user). Готово, если: таблицы остаются быстрыми на больших наборах (целевой бюджет: 10k+ строк), UX кастомизации детерминированный и консистентный между страницами, а a11y для таблиц проходит keyboard + screen reader sanity (roles/names, focus ring, shortcuts); DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                  |
| WEB-FRESH-001 | Реализовать data freshness + provenance индикаторы (P0)       | TODO   | Не начато | Реализация: Стандартизированный индикатор Freshness (last updated, staleness threshold, "refreshing") для Dashboard widgets и ключевых аналитических страниц. Добавить Provenance drawer: источник данных (scan run/job id), repo/branch/commit, data window, partial/failed flags, ссылка "open job/scan logs", CTA "Refresh / Rescan". Готово, если: пользователь всегда понимает насколько свежие данные он видит, stale/partial состояния не маскируются под success UI, а переход в диагностику/перезапуск доступен в 1-2 клика; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                   |
| WEB-TZ-001    | Реализовать timezone + schedule preview UX (P0)               | TODO   | Не начато | Реализация: Единый паттерн для всех расписаний (rescan schedule, report schedule): выбор timezone (user default + org override), human-friendly формулировки, preview следующих N запусков (с учетом DST), отображение абсолютного времени + относительного ("через 2ч"), и явное предупреждение о том, где применяется timezone (server vs user). Готово, если: расписание не двусмысленно, preview совпадает с backend расчётом на контрольных кейсах, и пользователь не может сохранить schedule без ясного указания timezone; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                       |
| WEB-XAI-001   | Реализовать explainability pattern для аналитики (P0)         | TODO   | Не начато | Реализация: Универсальный Explain drawer/panel для метрик/скорингов (predictions/drift/architecture health/impact): "Почему этот score?" (top factors, threshold, confidence, data window, ограничения), ссылки на первичные источники/срезы, и export explanation в report/snippet. Готово, если: у любого ключевого сигнала есть объяснимость на уровне пользователя без необходимости читать документацию/спрашивать саппорт, объяснение не перегружает (progressive disclosure), и базовые формулировки/лейблы локализуются; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                        |
| WEB-HELP-001  | Реализовать help & diagnostics center (P1)                    | TODO   | Не начато | Реализация: Встроенный Help Center (search + категории) и Diagnostics: checks для auth/session, network, provider connectivity, feature flags, browser/webgl readiness (для 3D), и генерация support bundle (redacted) для тикета. Deep-links из error states в релевантные статьи/диагностику. Готово, если: пользователь может самодиагностировать типовые проблемы (401/403, provider outage, scan failures) и получить понятный план действий, не теряя контекст текущей страницы; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                  |
| WEB-STATE-001 | Реализовать единый system empty/error states + microcopy (P1) | TODO   | Не начато | Реализация: Дизайн и реализация системных шаблонов состояний: empty (no data, filtered out, not onboarded), error (network, 401/403, 500, degraded), loading (skeleton), partial data (stale). Общие компоненты + гайд по microcopy и CTA, единый набор иконок/тональности. Готово, если: ключевые route-level страницы используют единые компоненты состояний, сообщения всегда предлагают следующий шаг (retry, open diagnostics, start onboarding), и regression в тексте/состояниях ловится тестами на snapshot/semantics; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                          |
| WEB-PERS-001  | Реализовать workspace personalization (P2)                    | TODO   | Не начато | Реализация: Персонализация рабочего пространства: сохранение default scope (org/repo/team), pinned shortcuts/виджеты на dashboard, сохранённые представления (views) для таблиц/фильтров, и (опционально) role-based preset layouts. Возможность share view (link) и sync между устройствами. Готово, если: пользователь может настроить UI под свой workflow без постоянных повторных кликов, настройки не ломают RBAC/tenant boundaries, и поведение предсказуемо при multi-tab; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                      |
| WEB-PAGE-025  | Реализовать usage & adoption analytics (value realization)    | TODO   | Не начато | Реализация: Страница для org admin: метрики adoption и time-to-first-value (фаннел: connect provider -> add repo -> first scan -> first insights -> first CCR reviewed), drop-offs, активные пользователи, ключевые действия, и health по workflow. Источник данных: события из `WEB-HOOK-007` (UX telemetry) + агрегаты backend. Готово, если: админ может понять “что мешает внедрению” без опросов пользователей, метрики привязаны к прозрачным определениям/окнам данных, и UI не нарушает privacy (нет содержимого кода/PII без явного opt-in); DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                   |

---
