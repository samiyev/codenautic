# M10 — UI Foundation & Dashboard

> Источник: `packages/ui/TODO.md`

> **Задач:** 74 | **Проверка:** Dashboard в браузере с реальными данными

> **Результат milestone:** Готов UI фундамент продукта: dashboard, settings, forms и ключевые пользовательские потоки.

## v0.0.0 — Базовая структура пакета

> Vite setup, Tailwind CSS, build infrastructure.

> **Результат версии:** Завершена версия «v0.0.0 — Package Foundation» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID      | Задача                               | Статус | Результат                                                                                                 | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------- | ------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-000 | Реализовать базовую структуру пакета | DONE   | Инициализирован bootstrap `src/main.tsx`, добавлены `src/routes/*`, сборка `vite build` проходит успешно. | Реализация: package.json, tsconfig.json, vite.config.ts, postcss.config.mjs, index.html, src/main.tsx, src/routes. Сборка проходит успешно. Готово, если: для WEB-000 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |

---

## v0.1.0 — Базовая конфигурация

> Vite setup, Tailwind, i18n, API client, testing, performance baseline. ~120K tokens.

> **Результат версии:** Завершена версия «v0.1.0 — Foundation» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID            | Задача                                                    | Статус | Результат                                                                                                                                                                                                                                                                                                                 | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------- | --------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-FOUND-001 | Реализовать настройку приложения Vite + TanStack Router   | DONE   | Поднят router (`src/app/router.ts`), root/index routes, `RouterProvider`, стартовый экран health-check.                                                                                                                                                                                                                   | Реализация: Настроены file-based routing, TypeScript, правила форматирования и ESLint/Prettier. Готово, если: для WEB-FOUND-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                      |
| WEB-FOUND-002 | Реализовать конфигурацию Tailwind CSS                     | DONE   | Добавлен `src/app/globals.css` с Tailwind 4 (`@import "tailwindcss"`), OKLch-дизайн-токенами, `dark` variant, базовыми reset/typography стилями и responsive-friendly global layout; подключено в `main.tsx`.                                                                                                             | Реализация: Настроены Tailwind 4, OKLch-токены, dark mode и адаптивность. Готово, если: для WEB-FOUND-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                            |
| WEB-FOUND-003 | Реализовать CSP и security-заголовки                      | DONE   | Добавлен централизованный `security-headers` модуль, подключены headers в `vite.config.ts` (`server` + `preview`), добавлены CSP/meta security теги в `index.html`, покрыто unit-тестами.                                                                                                                                 | Реализация: Content-Security-Policy. X-Frame-Options. HSTS. Готово, если: для WEB-FOUND-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                          |
| WEB-FOUND-004 | Реализовать i18n setup (i18next)                          | DONE   | Добавлен i18n-модуль (`i18next + react-i18next + language detector`) с persisted locale (`localStorage`), namespace-структурой `common/system` для `en` и `ru`, интеграцией в `SystemHealthPage` и Intl форматтерами даты/чисел.                                                                                          | Реализация: i18next + react-i18next. Locale detection и persist предпочтений. Namespace structure для `en` и `ru`. Intl formatting (dates, numbers). Готово, если: для WEB-FOUND-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-FOUND-005 | Реализовать font loading setup                            | DONE   | Настроены `@font-face` для web-fonts (`Geist Sans`/`Geist Mono`) с `font-display: swap`, fallback local/system fonts и metric overrides (`ascent/descent/size-adjust`) для снижения CLS; применено через глобальные font tokens.                                                                                          | Реализация: Настроить системные и web-fonts через CSS/font-face. CLS prevention. Proper font display strategy. Готово, если: для WEB-FOUND-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                       |
| WEB-FOUND-006 | Реализовать API client (fetch wrapper)                    | DONE   | Добавлены fetch-wrapper с экспоненциальным retry/backoff и 429 `Retry-After`, abort-aware ожиданием, типизированными `ApiHttpError/ApiRateLimitError/ApiNetworkError` + type guards, auth headers из `VITE_API_BEARER_TOKEN`, и расширенный контрактный/integration test coverage до 100% line/functions.                 | Реализация: Fetch wrapper (НЕ axios). Retry logic (exponential backoff). AbortController. Error type guards. Rate limiting (429). Auth headers. Готово, если: для WEB-FOUND-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.      |
| WEB-FOUND-007 | Реализовать провайдер React Query                         | DONE   | Добавлен `createQueryClient` с default options и подключен `QueryClientProvider` в app bootstrap.                                                                                                                                                                                                                         | Реализация: Настроен QueryClient, кэширование, background refetch, mutations и фабрика query keys. Готово, если: для WEB-FOUND-007 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                   |
| WEB-FOUND-008 | Реализовать error boundaries                              | DONE   | Добавлены глобальный и route-level fallback (`GlobalErrorFallback`, `RouteErrorFallback`, `NotFoundFallback`) с ветками 401/403/500 и retry, покрыто тестами конфигурации и поведения.                                                                                                                                    | Реализация: Глобальные и route-level error boundaries с fallback UI 401→login redirect. 403→access denied. 500→retry UI. Partial failure handling. Готово, если: для WEB-FOUND-008 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.   |
| WEB-FOUND-009 | Реализовать тестирование setup                            | DONE   | Добавлены MSW server mocks, `renderWithProviders`, app/page/query tests, исправлен `bun run test` script.                                                                                                                                                                                                                 | Реализация: @testing-library/react. @testing-library/user-event. MSW (Mock Service Worker). renderWithProviders utility. Test config in bunfig.toml. Готово, если: для WEB-FOUND-009 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-FOUND-010 | Реализовать bundle analyzer + perf budget                 | DONE   | Добавлен `build:analyze` с `rollup-plugin-visualizer` (`dist/bundle-analysis.json`), введен budget checker (`scripts/check-performance-budgets.mjs`) для порогов LCP/INP/CLS/JS-per-route по `perf/performance-budget.json` + baseline snapshot, и подключен CI job `UI Performance Budget` в `.github/workflows/ci.yml`. | Реализация: vite/rollup bundle analyzer. Performance budget: LCP < 2.5s, INP < 200ms, CLS < 0.1, JS < 200KB/route. CI check. Готово, если: для WEB-FOUND-010 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                         |
| WEB-FOUND-011 | Реализовать strategy границ UI-компонентов и lazy loading | DONE   | Описана стратегия eager/lazy и `Suspense` boundaries в `ARCHITECTURE.md`; главный route переведен на route-level lazy import (`React.lazy`) с `RouteSuspenseFallback`, добавлен тест fallback-компонента для стабильного coverage.                                                                                        | Реализация: Документ: какие компоненты грузятся eagerly/lazily, правила code-splitting по route. Suspense boundaries для тяжелых блоков. Готово, если: для WEB-FOUND-011 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.             |
| WEB-FOUND-012 | Реализовать настройку OpenAPI codegen                     | DONE   | Добавлены `codegen`/`codegen:check` scripts, автогенерация DTO перед `dev`/`build`, и CI job `UI OpenAPI Codegen Sync` для drift-check; контрактные типы продолжают генерироваться в `src/lib/api/generated/index.ts` из `openapi/schema.yaml`.                                                                           | Реализация: Генерация DTO типов из API OpenAPI schema. Автообновление при изменении schema. Shared types между API и web. Готово, если: для WEB-FOUND-012 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                            |

---

## v0.2.0 — Auth & Мониторинг

> Auth integration, Sentry, feature flags, Web Vitals. ~70K tokens.

> **Результат версии:** Завершена версия «v0.2.0 — Auth & Monitoring» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                                    | Статус | Результат                                                                                                                                                                                                                                                                                                                                                                                      | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                 |
| ------------ | ----------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-AUTH-001 | Реализовать web auth integration          | DONE   | Добавлены `AuthApi` endpoints (`session`, `oauth/start`, `session/refresh`, `logout`) с `credentials: include`, protected route boundary (`AuthBoundary`) для `/`, безопасный session snapshot без токенов в `sessionStorage`, auto-refresh near expiry, logout flow и OAuth redirect launch для GitHub/GitLab/Google/OIDC; покрыто auth unit/integration тестами и обновлёнными MSW handlers. | Реализация: OAuth (GitHub, GitLab, Google). OIDC. JWT/session cookies. Protected routes и refresh flow. Готово, если: OAuth/OIDC login, refresh и logout проходят e2e, защищённые роуты недоступны без сессии, токены/куки не утекают в клиентские логи и URL; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                          |
| WEB-AUTH-002 | Реализовать sentry (browser)              | DONE   | Добавлен runtime browser init Sentry в bootstrap (`initializeSentryBrowser`), trace/profile sample rate конфиг через `VITE_SENTRY_*`, environment/release нормализация, trace propagation targets для app/api origins, и data-scrubbing (`beforeSend`) для секретов в URL/headers/breadcrumbs; source maps upload уже обеспечивается `@sentry/vite-plugin` в `vite.config.ts`.                 | Реализация: Error tracking. Performance monitoring. Source maps upload. Готово, если: для WEB-AUTH-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-AUTH-003 | Реализовать web Vitals monitoring         | DONE   | Добавлен `web-vitals` monitoring модуль с отправкой `LCP`/`INP`/`CLS` в Sentry Performance как transaction events (`measurements` + `trace` context), idempotent-инициализацией и дедупликацией метрик по `name:id`; инициализация подключена в bootstrap после успешного `Sentry` init.                                                                                                       | Реализация: Core Web Vitals (LCP, INP, CLS) reporting через Sentry Performance. Готово, если: метрики LCP/INP/CLS отправляются в monitoring без дублей, есть проверка на отсутствие регрессии web vitals по контрольному сценарию; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                      |
| WEB-AUTH-004 | Реализовать feature flags setup           | DONE   | Добавлены API endpoint-контракт и React Query hook для загрузки feature flags (`/api/v1/feature-flags`) с polling refetch, реализован deny-by-default guard (`isFeatureFlagEnabled`) для pending/error состояний, и подключён premium feature gate в `SystemHealthPage`; при недоступности flags backend premium-функции остаются выключенными без перезагрузки страницы.                      | Реализация: Feature flags через React Query (server state). Gate premium features. Готово, если: feature flags переключают premium-функции без перезагрузки, есть deny-by-default поведение при недоступности flags backend; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                            |
| WEB-AUTH-005 | Реализовать проверку авторизации в layout | DONE   | Добавлен login route (`/login`) и guard в `AuthBoundary` для protected layout: неавторизованные пользователи редиректятся на login c `next`/`reason`, intended destination передаётся в OAuth `redirectUri`, после успешной сессии выполняется возврат в целевой route, а 401/403 состояния показываются явно в login panel.                                                                   | Реализация: Redirect unauthenticated users. Protected route wrapper. Готово, если: guard корректно редиректит неавторизованных пользователей и сохраняет intended destination после логина, 403/401 состояния отображаются явно; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                        |

---

## v0.3.0 — Дизайн-система

> HeroUI v3 (React Aria), Storybook, theming. ~80K tokens.

> **Результат версии:** Завершена версия «v0.3.0 — Design System» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID         | Задача                                                 | Статус | Результат                                                                                                                                                                                                 | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-DS-001 | Реализовать base UI components                         | DONE   | Реализовано: добавлены обёртки HeroUI v3 и базовый экспорт из `src/components/ui/*` для Button/Input/Select/Modal/Dropdown/Table/Card/Tabs.                                                               | Реализация: Button, Input, Select, Modal, Dropdown, Table, Card, Tabs. HeroUI v3 + React Aria patterns. A11y. Готово, если: для WEB-DS-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                 |
| WEB-DS-002 | Реализовать настройку Storybook                        | DONE   | Реализован Storybook 8 с базовым сетом stories для base components (`Button`, `Input`, `Select`, `Modal`, `Dropdown`, `Table`, `Card`, `Tabs`), глобальным переключателем темы и подключенным A11y addon. | Реализация: Storybook 8+. Stories для каждого base component. Dark mode toggle. A11y addon. Готово, если: для WEB-DS-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                   |
| WEB-DS-003 | Реализовать Theme provider (light/dark)                | DONE   | Реализовано: добавлен `ThemeProvider`, предзагрузка темы через `initializeTheme`, сохранение режима и реакция на `prefers-color-scheme`.                                                                  | Реализация: Light/dark/system mode. CSS variables (OKLch). Persisted preference. No flash on load. Готово, если: для WEB-DS-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                            |
| WEB-DS-004 | Реализовать cn() utility + shared patterns             | DONE   | Реализовано: добавлен utility `cn` в `src/lib/utils.ts`, единая схема классовых helpers для компонентов.                                                                                                  | Реализация: Clsx + tailwind-merge. Compound component pattern. Shared component conventions documented. Готово, если: для WEB-DS-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                       |
| WEB-DS-005 | Реализовать theme presets registry (HeroUI-like)       | DONE   | Реализован реестр `THEME_PRESETS` (6+ вариантов) с light/dark схемами на OKLch и централизованным применением CSS-переменных. Переключение preset не требует перезагрузки.                                | Реализация: Каталог именованных тем (light/dark пары) на основе OKLch токенов: минимум 6 пресетов с разными акцентами и контрастом, совместимых с HeroUI v3 theming. Готово, если: пользователь может переключать preset без перезагрузки и все базовые компоненты корректно переокрашиваются в обеих схемах; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                               |
| WEB-DS-006 | Реализовать global theme switcher UI                   | DONE   | Реализован `ThemeToggle` с `light/dark/system` и сеткой предпросмотра пресетов, включая `aria`-индикаторы и SSR-safe updates.                                                                             | Реализация: Глобальный переключатель темы как у HeroUI (mode: light/dark/system + grid предпросмотра пресетов) в header/user menu и быстрый доступ с клавиатуры. Готово, если: смена mode/preset отражается мгновенно, доступна клавиатурой и screen reader, состояние URL/route не ломается; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                               |
| WEB-DS-007 | Реализовать persistence and sync for theme preferences | DONE   | Реализованы localStorage persistence, cross-tab sync, API-persist профиля и deterministic конфликт-резолв по времени обновления; fallback в localStorage без потери состояния при недоступности API.      | Реализация: Сохранение выбранных mode/preset в профиле пользователя (через API) с fallback в localStorage, синхронизация между вкладками и корректный SSR/CSR hydration без flash. Готово, если: после logout/login и refresh тема восстанавливается, конфликт profile/local fallback разрешается детерминированно, нет визуальных артефактов при загрузке; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |

---

## v0.4.0 — Лэйаут

> App layout, sidebar, header, settings layout. ~70K tokens.

> **Результат версии:** Завершена версия «v0.4.0 — Layout» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID             | Задача                                   | Статус | Результат                                                                                                                              | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------- | ---------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-LAYOUT-001 | Реализовать app layout                   | DONE   | Реализован `DashboardLayout`, `SettingsLayout`, `MobileSidebar` и `Route`/`__root` структура для route-level lazy-loading.             | Реализация: Main layout structure. Route-level layouts с ленивой загрузкой страниц и секций. Готово, если: для WEB-LAYOUT-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-LAYOUT-002 | Реализовать sidebar navigation           | DONE   | Реализована панель навигации с collapsible `Sidebar`, desktop/mobile версиями и вложенной навигацией через `SidebarNav`/`SettingsNav`. | Реализация: Collapsible sidebar. Navigation items. Active state. Responsive. Готово, если: для WEB-LAYOUT-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                 |
| WEB-LAYOUT-003 | Реализовать header with user menu        | DONE   | Реализован `Header` с hero-ui `Navbar`, `UserMenu`, `ThemeToggle` и indicator уведомлений. Добавлена мобильная кнопка открытия drawer. | Реализация: User avatar. Dropdown menu. Notifications indicator. Готово, если: для WEB-LAYOUT-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                             |
| WEB-LAYOUT-004 | Реализовать loading states (route-level) | DONE   | Добавлены `Suspense` + `RouteSuspenseFallback` и skeleton-компоненты в роутах dashboard/settings/reviews.                              | Реализация: Skeleton loaders. Suspense boundaries per-section. Готово, если: для WEB-LAYOUT-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                               |
| WEB-LAYOUT-005 | Реализовать settings layout              | DONE   | Реализован `SettingsLayout` с боковой навигацией по подстраницам и nested маршрутизацией настроек через TanStack Router.               | Реализация: Sidebar/tabs layout для 15+ settings подстраниц. Nested routing. Готово, если: для WEB-LAYOUT-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                 |

---

## v0.5.0 — Forms & List Инфраструктура

> react-hook-form, Zod, virtual scrolling, infinite scroll, search hooks. ~90K tokens.

> **Результат версии:** Завершена версия «v0.5.0 — Forms & List Infrastructure» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                                   | Статус | Результат                                                                                                                                                     | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                       |
| ------------ | ---------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-FORM-001 | Реализовать form components              | DONE   | Реализованы обёртки RHF (`FormTextField`, `FormSelectField` и др.), общий `FormSubmitButton`, unit-тесты и документация через `src/components/forms/*`.       | Реализация: React-hook-form integration. Reusable form field components. Готово, если: для WEB-FORM-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.      |
| WEB-FORM-002 | Реализовать zod schema validation        | DONE   | Реализован `schema-validation` модуль с `parseSchemaOrError` и общими тестами (`schema-validation.test.ts`) для унифицированной клиентской валидации форм.    | Реализация: Client-side validation. Zod schemas shared with API where needed. Готово, если: для WEB-FORM-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-FORM-003 | Реализовать enum validation              | DONE   | Реализованы `createEnumSchema`, `parseEnumValue`, `isEnumValue` с покрытием для enum-полей форм и shared tests в `settings-form-schemas`/`schema-validation`. | Реализация: Validate enum values from API types. Готово, если: для WEB-FORM-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                              |
| WEB-FORM-004 | Реализовать input sanitization           | DONE   | Реализован sanitize-пайплайн (`sanitizeText`, `createSanitizedStringSchema`) на `isomorphic-dompurify` и покрытие в тестах XSS- и trim-сценариев.             | Реализация: XSS prevention. DOMPurify or similar. Готово, если: для WEB-FORM-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                             |
| WEB-VIRT-001 | Реализовать tanStack Virtual setup       | DONE   | Реализован `useVirtualizedList` на `@tanstack/react-virtual` в `src/lib/hooks/use-virtualized-list.ts` и подключаемые контейнеры в компонентах списков.       | Реализация: @tanstack/react-virtual configured. Base virtualized list/table. Готово, если: для WEB-VIRT-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.  |
| WEB-INF-001  | Реализовать useIntersectionObserver hook | DONE   | Реализован `useIntersectionObserver` в `src/lib/hooks/use-intersection-observer.ts`, покрыт интеграцией в `InfiniteScrollContainer`.                          | Реализация: Generic IntersectionObserver hook. Готово, если: для WEB-INF-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                 |
| WEB-INF-002  | Реализовать infiniteScrollContainer      | DONE   | Реализован `InfiniteScrollContainer` с sentinel и флагами `hasMore/isLoading`, используемый в review screens.                                                 | Реализация: Generic infinite scroll wrapper. React Query integration. Готово, если: для WEB-INF-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.          |
| WEB-HOOK-001 | Реализовать useDebounce()                | DONE   | Реализован `useDebounce` и `useDebounceWithOptions` в `src/lib/hooks/use-debounce.ts`, применяемые в `ReviewsContent`.                                        | Реализация: Generic debounce hook. Готово, если: для WEB-HOOK-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                            |
| WEB-HOOK-002 | Реализовать useDebouncedSearch()         | DONE   | Реализован `useDebouncedSearch` с React Query (включая `queryKey`, `queryFn`, `allowEmpty` и отдачу `debouncedSearch`).                                       | Реализация: Debounced search hook (400ms). React Query integration. Готово, если: для WEB-HOOK-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.           |

---

## v0.6.0 — Core Pages

> Dashboard, CCR management, Settings. ~100K tokens.

> **Результат версии:** Завершена версия «v0.6.0 — Core Pages» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID           | Задача                          | Статус | Результат                                                                                                                                              | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------ | ------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-PAGE-001 | Реализовать панель управления   | DONE   | Реализован `DashboardMissionControlPage` и связанный `DashboardContent`/`MetricsGrid`/`DashboardDateRangeFilter` с deep-link и сигнальными карточками. | Реализация: Dashboard Mission Control. Scope bar (org/repo/team) + date range. KPI strip + activity timeline. Work Queue (CCRs, issues, notifications preview) с deep-link в целевые экраны. Signals (drift/predictions/architecture health) + Explore links (CodeCity/Graph/Causal/Impact/Refactor/Knowledge/Reports). Ops strip (jobs/provider health/degraded banner). Usage & plan summary (token usage + entitlement banner). Suspense boundaries per widget. Route-level lazy loading для тяжелых виджетов. Готово, если: dashboard позволяет за 1-2 клика перейти из сигнала/очереди в конкретный контекст (review/issue/job/drift/city/graph/impact/refactor/report), виджеты уважают RBAC/tenant context и имеют loading/empty/error states, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-PAGE-002 | Реализовать CCR management      | DONE   | Реализован `CcrManagementPage` с фильтрами (`team`,`repo`,`status`,`search`), URL-синхронизацией (`TanStack Router`) и infinite-like подгрузкой.       | Реализация: List with filters (team, repo, status). Virtual scrolling. Infinite scroll. URL search params через TanStack Router (shareable filters). Готово, если: для WEB-PAGE-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| WEB-PAGE-003 | Реализовать code Review config  | DONE   | Реализована страница `SettingsCodeReviewPage` с `CodeReviewForm`, игнор-путями и обработкой обновлений состояния.                                      | Реализация: Review cadence. Ignore paths. Severity filters. Suggestion limits. Готово, если: для WEB-PAGE-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| WEB-PAGE-004 | Реализовать LLM provider config | DONE   | Реализована страница `SettingsLlmProvidersPage` с `LlmProviderForm`, тестами подключения и выбором провайдера/модели.                                  | Реализация: Select provider. BYOK key input. Model selection. Test connection. Готово, если: для WEB-PAGE-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| WEB-PAGE-005 | Реализовать git provider config | DONE   | Реализована страница `SettingsGitProvidersPage` со списком подключений и тестированием подключений/переключением статусов.                             | Реализация: Connect GitHub/GitLab/Azure/Bitbucket. OAuth flow. Webhook setup. Готово, если: для WEB-PAGE-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

## v0.6.1 — UI Stack Migration: Настройка & Инфраструктура

> Инициализация HeroUI v3, удаление Mantine/shadcn/Radix, обновление провайдеров. ~40K tokens.

> **Результат версии:** Завершена версия «v0.6.1 — UI Stack Migration: Setup & Infrastructure» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID          | Задача                                      | Статус | Результат                                                                                                                                                                                         | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------- | ------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-MIG-001 | Реализовать brand guidelines merge          | DONE   | Реализован `.skills/brand-guidelines/SKILL.md` с объединением OpenPayment-ориентированных и HeroUI-паттернов: градиенты, кнопки/карточки, форматирование метрик, provider colors и status colors. | Реализация: Мердж OpenPayment стилей в SKILL.md. Gradients, button/card styles, metric formatting, provider colors, status colors. HEX → OKLch конвертация. Готово, если: для WEB-MIG-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                            |
| WEB-MIG-002 | Реализовать initialize HeroUI v3            | DONE   | Подключены `@heroui/react/@heroui/styles`, импорты базовых провайдеров в bootstrape (`ThemeProvider`/App).                                                                                        | Реализация: Установить `@heroui/react` и базовую конфигурацию HeroUI v3 для Vite + Tailwind v4. Подключить provider в app bootstrap. Готово, если: для WEB-MIG-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                   |
| WEB-MIG-003 | Реализовать install HeroUI v3 components    | DONE   | Добавлены и используются обёртки HeroUI для ключевых компонентов (`Button`, `Input`, `Select`, `Modal`, `Dropdown`, `Table`, `Card`, `Tabs` и др.) в `src/components/ui`.                         | Реализация: Button, Card, Input, Select, Textarea, Checkbox, Switch, Avatar, Chip, Alert, Tooltip, Dropdown, Drawer, Skeleton, RadioGroup, Tabs. Готово, если: для WEB-MIG-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                       |
| WEB-MIG-004 | Реализовать remove legacy UI dependencies   | DONE   | Удалены/не подключаются Mantine/shadcn/radix/CVA-зависимости в `package.json` и UI-коде; используется HeroUI + tailwind-only стек.                                                                | Реализация: Удалить @mantine/core, @mantine/charts, @mantine/hooks, @mantine/notifications, postcss-preset-mantine и неиспользуемые зависимости shadcn/Radix/CVA слоя (`class-variance-authority`, `radix-ui`, `clsx`, `tailwind-merge` при отсутствии реальных импортов). Удалить/заменить `components.json` с `ui.shadcn.com/schema.json`. Обновить globals.css, postcss.config.mjs. Готово, если: для WEB-MIG-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-MIG-005 | Реализовать unified toasts for HeroUI stack | DONE   | Реализован HeroUI `Toast.Provider` и единый wrapper `showToast*` для success/info/warn/error, миграция всех локальных toast-вызовов в settings-страницы с удалением `sonner`.                     | Реализация: Интегрировать единый toast-слой совместимый с HeroUI v3 и заменить текущий legacy provider. Готово, если: для WEB-MIG-005 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                                |
| WEB-MIG-006 | Реализовать миграцию theme provider         | DONE   | Реализован `ThemeProvider` в `src/lib/theme/theme-provider.tsx` без Mantine-слоя; layout и app используют только HeroUI-совместимый контекст.                                                     | Реализация: Убрать MantineProvider/legacy wrappers, настроить HeroUI v3 theming без обязательного provider + `next-themes`, обновить theme-provider.tsx и layout.tsx. Готово, если: для WEB-MIG-006 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                  |
| WEB-MIG-007 | Реализовать обновление test render utility  | DONE   | Обновлён `tests/utils/render.tsx` для `ThemeProvider`+`QueryClientProvider` без Mantine wrappers.                                                                                                 | Реализация: Обновить tests/utils/render.tsx — убрать MantineProvider/legacy wrappers и привести test wrapper к HeroUI v3 (без обязательного provider, с корректным theme context). Готово, если: для WEB-MIG-007 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                     |
| WEB-MIG-008 | Реализовать storybook provider update       | DONE   | Обновлён `.storybook/preview.tsx` под `ThemeProvider` и `QueryClientProvider`, сохранён i18n init и параметры a11y/storybook.                                                                     | Реализация: Обновить `.storybook/preview.tsx` — убрать Mantine/shadcn providers, добавить HeroUI theming и providers. Готово, если: для WEB-MIG-008 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                                                                                                  |

---

## v0.6.2 — UI Stack Migration: Components

> Замена Mantine/shadcn компонентов на HeroUI v3 и Tailwind. ~100K tokens.

> **Результат версии:** Завершена версия «v0.6.2 — UI Stack Migration: Components» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID          | Задача                                             | Статус | Результат                                                                                                                                                        | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------- | -------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-MIG-009 | Реализовать layout components migration            | DONE   | Реализованы `Sidebar`, `Header`, `DashboardLayout`, `MobileSidebar`, `SettingsNav`, `UserMenu`, `ThemeToggle` на HeroUI и общих паттернах.                       | Реализация: Sidebar.tsx, sidebar-nav.tsx, header.tsx, dashboard-layout.tsx, mobile-sidebar.tsx, settings-nav.tsx, user-menu.tsx, theme-toggle.tsx. Layout-компоненты переводятся на HeroUI Navbar/Drawer/Dropdown/Button паттерны с сохранением A11y. Готово, если: для WEB-MIG-009 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                         |
| WEB-MIG-010 | Реализовать form components migration              | DONE   | Реализованы form-компоненты из HeroUI (`FormTextField`, `FormTextareaField`, `FormSelectField` и др.) в `src/components/forms`.                                  | Реализация: Form-text-field, form-textarea-field, form-select-field, form-number-field, form-password-field, form-checkbox-field, form-switch-field, form-radio-group-field, form-submit-button. Form controls переводятся на HeroUI Input/Select/Checkbox/Switch/RadioGroup. Готово, если: для WEB-MIG-010 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-MIG-011 | Реализовать панель управления components migration | DONE   | Реализованы `dashboard-content`, `metric-card`, `metrics-grid`, `activity-timeline*`, `dashboard-date-range-filter` на Tailwind + HeroUI.                        | Реализация: Metric-card, metrics-grid, dashboard-content, activity-timeline, activity-timeline-item, dashboard-date-range-filter. Card→HeroUI Card, Badge→HeroUI Chip, Text→Tailwind, Title→h1-h6, Stack→flex, Grid→grid. Готово, если: для WEB-MIG-011 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd/packages/ui && bun run lint && bun run typecheck && bun run test`.                                                     |
| WEB-MIG-012 | Реализовать status distribution chart migration    | DONE   | Реализована замена на Recharts `PieChart` в `status-distribution-chart.tsx` с цветами из дашборда.                                                               | Реализация: Заменить @mantine/charts PieChart на Recharts PieChart. Файл: status-distribution-chart.tsx. Готово, если: для WEB-MIG-012 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                      |
| WEB-MIG-013 | Реализовать reviews components migration           | DONE   | Реализованы `reviews-content`, `reviews-table`, `reviews-filters`, `review-status-badge` на HeroUI Table/Chip/Select; добавлен локальный data path и фильтрация. | Реализация: Reviews-content, reviews-table, reviews-filters, review-status-badge. Table→HeroUI Table, Badge→HeroUI Chip, Select→HeroUI Select, Text→Tailwind. Готово, если: для WEB-MIG-013 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                 |
| WEB-MIG-014 | Реализовать settings components migration          | DONE   | Реализованы `git-provider-card`, `git-providers-list`, `llm-provider-form`, `test-connection-button`, `code-review-form`, `ignore-paths-editor` в HeroUI-стиле.  | Реализация: Git-provider-card, git-providers-list, llm-provider-form, test-connection-button, code-review-form, ignore-paths-editor. Card→Card, Button→Button, Alert→Alert, TextInput→Input. Готово, если: для WEB-MIG-014 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                  |
| WEB-MIG-015 | Реализовать settings pages migration               | DONE   | Реализованы страницы settings (`/settings`, `/settings/git-providers`, `/settings/llm-providers`, `/settings/code-review`) с flex/hero-style и lazy-роутингом.   | Реализация: Settings/layout.tsx, git-providers/page.tsx, llm-providers/page.tsx, code-review/page.tsx. Stack→flex, Title→h1-h6. Готово, если: для WEB-MIG-015 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                               |
| WEB-MIG-016 | Реализовать skeleton components migration          | DONE   | Реализованы `dashboard-skeleton`, `settings-skeleton`, `reviews-skeleton` на HeroUI `Skeleton`.                                                                  | Реализация: Панель управления-skeleton, settings-skeleton, reviews-skeleton. Mantine/shadcn Skeleton→HeroUI Skeleton, Stack→flex, Grid→grid. Готово, если: для WEB-MIG-016 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                  |

---

## v0.6.3 — UI Stack Migration: Тесты & Verification

> Обновление тестов, lint, build, финальная проверка. ~60K tokens.

> **Результат версии:** Завершена версия «v0.6.3 — UI Stack Migration: Tests & Verification» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID          | Задача                                    | Статус | Результат                                                                                                         | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------- | ----------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-MIG-017 | Реализовать layout component tests update | DONE   | Обновлены component tests: Header, Sidebar, SidebarNav, MobileSidebar, ThemeToggle, DashboardLayout, SettingsNav. | Реализация: Обновить. Готово, если: для WEB-MIG-017 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                      |
| WEB-MIG-018 | Реализовать form component tests update   | DONE   | Добавлены обновлённые tests для form-controls и submit-состояния, включая валидацию и send payload.               | Реализация: Обновить. Готово, если: для WEB-MIG-018 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                      |
| WEB-MIG-019 | Реализовать lint and format               | DONE   | Выполнены `bun run format` и `bun run lint`; закрыты все `@typescript-eslint` ошибки по блокерным страницам/формам, включая `schema-validation` и stories/ccr pages. | Реализация: `bun run format` + `bun run lint` — 0 ошибок, 0 предупреждений. Готово, если: для WEB-MIG-019 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                |
| WEB-MIG-020 | Реализовать typecheck                     | DONE   | `bun run typecheck` выполнен без ошибок в рамках закрытия milestone `v0.6.3`.                                 | Реализация: `bun run typecheck` — без ошибок. Готово, если: для WEB-MIG-020 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                              |
| WEB-MIG-021 | Реализовать all tests passing             | DONE   | Обновлены layout-тесты и глобальные моки для стабильного прохождения `bun run test`.                                   | Реализация: `bun test` — все тесты зелёные. Покрытие ≥ 95%. Готово, если: для WEB-MIG-021 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                |
| WEB-MIG-022 | Реализовать build verification            | TODO   | Не начато                                                                                                         | Реализация: `bun run build` — успешная сборка. Нет Mantine/shadcn/Radix импортов в бандле. Готово, если: для WEB-MIG-022 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |

---

## v0.7.0 — Панель управления Widgets

> Stats cards, charts, timeline, indicators. ~80K tokens.

> **Результат версии:** Завершена версия «v0.7.0 — Dashboard Widgets» в рамках M10; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID            | Задача                                        | Статус | Результат | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------- | --------------------------------------------- | ------ | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WEB-DASH-001  | Реализовать панель управления stats cards     | TODO   | Не начато | Реализация: Deploy frequency, CCR cycle time, suggestions, bug ratio, CCR size. Готово, если: для WEB-DASH-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                             |
| WEB-DASH-002  | Реализовать activity timeline                 | TODO   | Не начато | Реализация: Recent reviews. Grouped by day. Expandable details. Готово, если: для WEB-DASH-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                             |
| WEB-COMP-001  | Реализовать recharts wrapper                  | TODO   | Не начато | Реализация: Recharts wrapper component. Responsive. Loading state. Theme-aware. Готово, если: для WEB-COMP-001 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                             |
| WEB-CHART-001 | Реализовать chart scale policy + downsampling | TODO   | Не начато | Реализация: Политика масштабирования графиков: пороги точек, downsampling (например LTTB/binning), серверная агрегация при больших диапазонах, и явный UI-индикатор “data aggregated” + экспорт raw CSV. Интеграция в `WEB-COMP-001` wrapper, theme-aware. Готово, если: графики остаются интерактивными на длинных диапазонах (без фризов), пользователь понимает степень агрегации и может получить исходные данные, а поведение детерминировано (одинаковый input -> одинаковый output); DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`. |
| WEB-CHART-002 | Реализовать chart perf + regression harness   | TODO   | Не начато | Реализация: Набор perf/regression кейсов для графиков (5k/10k/50k points, разные типы виджетов), автоматические проверки “no layout break” на длинных локализациях, и профилирование render budget. Готово, если: есть воспроизводимые кейсы, которые ловят регрессии в производительности/верстке до merge, а результаты понятны (thresholds + артефакты); DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                 |
| WEB-COMP-002  | Реализовать metric card                       | TODO   | Не начато | Реализация: Value, label, trend, sparkline. Configurable color. Готово, если: для WEB-COMP-002 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                             |
| WEB-COMP-003  | Реализовать trend indicator                   | TODO   | Не начато | Реализация: Up/down arrow. Percentage change. Color coding. Accessible text. Готово, если: для WEB-COMP-003 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                |
| WEB-COMP-004  | Реализовать провайдер connection card         | TODO   | Не начато | Реализация: Status indicator. Connect/disconnect. Last sync. Loading state. Готово, если: для WEB-COMP-004 ключевой пользовательский флоу проходит end-to-end в UI без ошибок, есть component + integration покрытие, проверены a11y (keyboard/focus/aria/contrast) и responsive для mobile/tablet/desktop; DoD: `cd packages/ui && bun run lint && bun run typecheck && bun run test`.                                                                                                                                                                                 |

---
