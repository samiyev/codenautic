# M12 — AST, Scan, Onboarding

> Источник: `packages/core/TODO.md`

> **Задач (core):** 30 | **Проверка:** CodeCity domain, repository onboarding domain

> **Результат milestone:** Готов доменный слой CodeCity и onboarding для масштабного анализа кодовой базы.

## v0.48.0 — CodeCity Domain

> DTO, порты и use case для CodeCity визуализации (2D treemap + 3D city). Per-file метрики, treemap layout data, issue

> **Результат версии:** Завершена версия «v0.48.0 — CodeCity Domain» в рамках M12; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.
> heatmap aggregation.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-253 | Реализовать fileMetrics DTO | DONE | Реализовано | Реализация: `application/dto/analytics/file-metrics.dto.ts`. Interface: `{filePath: string, loc: number, complexity: number, coverage?: number, churn: number, issueCount: number, lastReviewDate?: string}`. Extends IDTO. Тесты типов. Готово, если: для CORE-253 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-254 | Реализовать treemapNode DTO | DONE | Реализовано | Реализация: Interface с полями `id`, `name`, `type` (`file` или `directory`), `metrics`, `children`. Рекурсивная структура. Extends IDTO. Тесты типов. Готово, если: для CORE-254 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-255 | Реализовать issueHeatmapEntry DTO | DONE | Реализовано | Реализация: `application/dto/analytics/issue-heatmap-entry.dto.ts`. Interface: `{filePath: string, totalIssues: number, bySeverity: Record<string, number>, byCategory: Record<string, number>}`. Extends IDTO. Тесты типов. Готово, если: для CORE-255 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-256 | Реализовать codeCityData DTO | DONE | Реализовано | Реализация: `application/dto/analytics/code-city-data.dto.ts`. Interface: `{repositoryId: string, rootNode: TreemapNode, heatmap: IssueHeatmapEntry[], hotspots: {filePath: string, score: number}[], generatedAt: string}`. Aggregate DTO для фронтенда. Реализован отдельный интерфейс `IHotspotMetric`, добавлены экспорт через barrel `analytics/index.ts` и root export, интеграция в публичный API, покрытия тестами. Реализовано: `packages/core/tests/application/dto/analytics/code-city-data.dto.test.ts`. Готово, если: для CORE-256 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-257 | Реализовать iFileMetricsProvider порт | DONE | Реализовано | Реализация: `application/ports/outbound/analysis/file-metrics-provider.ts`. Interface: `getMetrics(repoId: string, filePaths: string[]): Promise<readonly IFileMetricsDTO[]>` . Токен `TOKENS.Analysis.FileMetricsProvider`. Обновлён публичный экспорт из core API, добавлен контрактный тест `packages/core/tests/application/ports/outbound/file-metrics-provider.port.test.ts`, токен подтверждён в `packages/core/tests/ioc/tokens.test.ts`, проверка покрытия lint/typecheck/test выполнена и завершена успешно. |
| CORE-258 | Реализовать iIssueAggregationProvider порт | DONE | Реализовано | Реализация: `application/ports/outbound/review/issue-aggregation-provider.ts`. Interface: `aggregateByFile(repoId: string): Promise<readonly IIssueHeatmapEntryDTO[]>`. Токен `TOKENS.Review.IssueAggregationProvider`. Обновлён публичный экспорт из core API, добавлен контрактный тест `packages/core/tests/application/ports/outbound/issue-aggregation-provider.port.test.ts`, токен подтверждён в `packages/core/tests/ioc/tokens.test.ts`, проверка покрытия lint/typecheck/test выполнена и завершена успешно. |
| CORE-259 | Реализовать getCodeCityDataUseCase | TODO | Не начато | Реализация: `application/use-cases/analytics/get-code-city-data.use-case.ts`. IUseCase<{repoId, branch?}, CodeCityData>. Загружает CodeGraph (IGraphRepository) → строит TreemapNode иерархию → обогащает FileMetrics (IFileMetricsProvider) → aggregates issues (IIssueAggregationProvider) → hotspots из PageRank. Готово, если: для CORE-259 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-260 | Реализовать getTemporalDiffUseCase | TODO | Не начато | Реализация: `application/use-cases/analytics/get-temporal-diff.use-case.ts`. IUseCase<{repoId, fromCommit, toCommit}, {added: TreemapNode[], removed: TreemapNode[], changed: {node: TreemapNode, metricsDelta: Partial<FileMetrics>}[]}>. Использует IDependencyGraphService + IGraphRepository. Готово, если: для CORE-260 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.49.0 — Репозиторий Onboarding: Ports & DTOs

> Расширение IGitProvider, новые порты для сканирования репозитория, DTO для прогресса и результатов.

> **Результат версии:** Завершена версия «v0.49.0 — Repository Onboarding: Ports & DTOs» в рамках M12; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-261 | Реализовать расширение IGitProvider: getFileTree() | TODO | Не начато | Реализация: добавить DTO `IFileTreeNode` с полями `path`, `type` (`blob` или `tree`), `size`, `sha`. Barrel export. Готово, если: для CORE-261 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-262 | Реализовать расширение IGitProvider: getFileContentByRef() | TODO | Не начато | Реализация: Добавить в IGitProvider метод `getFileContentByRef(filePath: string, ref: string): Promise<string>`. Не требует mergeRequestId, работает напрямую с ref (branch/commit SHA). Готово, если: для CORE-262 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-263 | Реализовать расширение IGitProvider: GetCommitHistory() | TODO | Не начато | Реализация: Добавить метод `GetCommitHistory(ref: string, options?: ICommitHistoryOptions): Promise<ICommitInfo[]>`. DTO `ICommitInfo`: `{sha, message, authorName, authorEmail, date, filesChanged}`. DTO `ICommitHistoryOptions`: `{since?, until?, maxCount?, filePath?}`. Barrel export. Готово, если: для CORE-263 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-264 | Реализовать расширение IGitProvider: getBranches() | TODO | Не начато | Реализация: Добавить метод `getBranches(): Promise<IBranchInfo[]>`. DTO `IBranchInfo`: `{name, sha, isDefault, isProtected}`. Barrel export. Готово, если: для CORE-264 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-265 | Реализовать расширение IGitProvider: getBlameData() | TODO | Не начато | Реализация: Добавить метод `getBlameData(filePath: string, ref: string): Promise<IBlameData[]>`. DTO `IBlameData`: `{lineStart, lineEnd, commitSha, authorName, authorEmail, date}`. Barrel export. Готово, если: для CORE-265 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-266 | Реализовать scanProgress DTO | TODO | Не начато | Реализация: `application/dto/scanning/scan-progress.dto.ts`. `IScanProgress`: `{scanId, repositoryId, status: ScanStatus, totalFiles, processedFiles, currentPhase: ScanPhase, startedAt, updatedAt, errorMessage?}`. ScanStatus: PENDING/SCANNING_FILES/BUILDING_GRAPH/COMPUTING_METRICS/COMPLETED/FAILED. ScanPhase: FILE_DISCOVERY/FILE_PARSING/GRAPH_BUILDING/METRICS_COMPUTATION/FINALIZATION. Тесты типов. Готово, если: для CORE-266 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-267 | Реализовать scanResult DTO | TODO | Не начато | Реализация: `application/dto/scanning/scan-result.dto.ts`. `IScanResult`: `{scanId, repositoryId, totalFiles, totalNodes, totalEdges, languages: ILanguageStat[], duration, completedAt}`. `ILanguageStat`: `{language, fileCount, loc}`. Тесты типов. Готово, если: для CORE-267 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-268 | Реализовать репозиторийIndex DTO | TODO | Не начато | Реализация: `application/dto/scanning/repository-index.dto.ts`. `IRepositoryIndex`: `{repositoryId, defaultBranch, lastScanId?, lastScanAt?, totalFiles, totalLoc, languages: ILanguageStat[], status: RepositoryIndexStatus}`. RepositoryIndexStatus: NOT_INDEXED/INDEXING/INDEXED/STALE. Тесты типов. Готово, если: для CORE-268 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.50.0 — Репозиторий Onboarding: Domain Events & Ports

> Domain events для сканирования, IRepositoryScanner порт, IRepositoryIndexRepository порт.

> **Результат версии:** Завершена версия «v0.50.0 — Repository Onboarding: Domain Events & Ports» в рамках M12; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-269 | Реализовать scanStarted domain event | TODO | Не начато | Реализация: `domain/events/scan-started.ts`. Extends BaseDomainEvent. Props: `repositoryId, scanId, triggeredBy`. eventName = "ScanStarted". Готово, если: для CORE-269 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-270 | Реализовать scanCompleted domain event | TODO | Не начато | Реализация: `domain/events/scan-completed.ts`. Extends BaseDomainEvent. Props: `repositoryId, scanId, totalFiles, totalNodes, duration`. eventName = "ScanCompleted". Barrel export. Готово, если: для CORE-270 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-271 | Реализовать scanFailed domain event | TODO | Не начато | Реализация: `domain/events/scan-failed.ts`. Extends BaseDomainEvent. Props: `repositoryId, scanId, errorMessage, phase: ScanPhase`. eventName = "ScanFailed". Barrel export. Готово, если: для CORE-271 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-272 | Реализовать репозиторийIndexed domain event | TODO | Не начато | Реализация: `domain/events/repository-indexed.ts`. Extends BaseDomainEvent. Props: `repositoryId, totalFiles, languages: string[]`. eventName = "RepositoryIndexed". Barrel export. Готово, если: для CORE-272 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-273 | Реализовать iRepositoryScanner порт | TODO | Не начато | Реализация: `application/ports/outbound/scanning/repository-scanner.ts`. `interface IRepositoryScanner { scanRepository(repoId, ref, onProgress?): Promise<IScanResult>; cancelScan(scanId): Promise<void> }`. Токен `TOKENS.Scanning.RepositoryScanner`. Barrel export. Готово, если: для CORE-273 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-274 | Реализовать iRepositoryIndexRepository порт | TODO | Не начато | Реализация: Null>; updateStatus(repoId, status): Promise<void>; updateLastScan(repoId, scanId, scanAt): Promise<void> }`. Токен `TOKENS.Scanning.РепозиторийIndexRepository`. Barrel export. Готово, если: для CORE-274 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-275 | Реализовать iScanProgressRepository порт | TODO | Не начато | Реализация: `application/ports/outbound/scanning/scan-progress-repository.ts`. `interface IScanProgressRepository { save, findByScanId, findByRepoId, updateProgress(scanId, processedFiles, phase), markCompleted(scanId), markFailed(scanId, error) }`. Токен `TOKENS.Scanning.ScanProgressRepository`. Barrel export. Готово, если: для CORE-275 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-276 | Реализовать tOKENS.Scanning namespace | TODO | Не начато | Реализация: Добавить в `infrastructure/ioc/tokens.ts` namespace `Scanning` с токенами: RepositoryScanner, РепозиторийIndexRepository, ScanProgressRepository. Готово, если: для CORE-276 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.51.0 — Репозиторий Onboarding: Use Cases

> Оркестрация сканирования, индексации и реиндексации.

> **Результат версии:** Завершена версия «v0.51.0 — Repository Onboarding: Use Cases» в рамках M12; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-277 | Реализовать indexRepositoryUseCase | TODO | Не начато | Реализация: `application/use-cases/scanning/index-repository.use-case.ts`. IUseCase<{repoId, triggeredBy}, IRepositoryIndex, DomainError>. Оркестрирует: getBranches → создаёт ScanProgress → ScanRepositoryUseCase → BuildFullGraphUseCase → сохраняет Index → RepositoryIndexed event. При ошибке — ScanFailed event. Готово, если: для CORE-277 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-278 | Реализовать scanRepositoryUseCase | TODO | Не начато | Реализация: `application/use-cases/scanning/scan-repository.use-case.ts`. IUseCase<{repoId, ref, scanId}, IScanResult, DomainError>. getFileTree → батчами getFileContentByRef (100 файлов) → IRepositoryScanner.scanRepository() с progress callback → ScanStarted/ScanCompleted events. Готово, если: для CORE-278 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-279 | Реализовать buildFullGraphUseCase | TODO | Не начато | Реализация: `application/use-cases/scanning/build-full-graph.use-case.ts`. IUseCase<{repoId, ref, scanResult}, ICodeGraph, DomainError>. IDependencyGraphService.buildGraph() с полным набором файлов → IGraphRepository.saveGraph() → GraphUpdated event. Готово, если: для CORE-279 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-280 | Реализовать reindexRepositoryUseCase | TODO | Не начато | Реализация: `application/use-cases/scanning/reindex-repository.use-case.ts`. IUseCase<{repoId, ref, triggeredBy}, IRepositoryIndex, DomainError>. Проверяет нет ли активного скана (ConflictError) → обновляет status INDEXING → делегирует IndexRepositoryUseCase. Готово, если: для CORE-280 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-281 | Реализовать getScanStatusUseCase | TODO | Не начато | Реализация: `application/use-cases/scanning/get-scan-status.use-case.ts`. IUseCase<{scanId}, IScanProgress, DomainError>. Загружает IScanProgress. NotFoundError если не найден. Готово, если: для CORE-281 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-282 | Реализовать getRepositoryIndexUseCase | TODO | Не начато | Реализация: `application/use-cases/scanning/get-repository-index.use-case.ts`. IUseCase<{repoId}, IRepositoryIndex, DomainError>. Загружает IRepositoryIndex. NotFoundError если не indexed. Готово, если: для CORE-282 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---
